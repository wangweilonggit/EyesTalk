<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="@dimen/subcategory_first_listitem_width_port_mode"
    android:layout_height="match_parent"
    android:background="@color/choose_lens_sub_bar_first_bg" >

    <TextView
        android:id="@+id/textview_title_in_subcategory_item"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:gravity="center"
        android:textColor="@color/white"
        android:visibility="gone" />

    <ImageView
        android:id="@+id/imageview_content_in_subcategory_item"
        android:layout_width="@dimen/subcategory_first_listitem_width_port_mode"
        android:layout_height="match_parent"
        android:layout_above="@+id/textview_title_in_subcategory_item"
        android:adjustViewBounds="true"
        android:background="@color/transparent"
        android:contentDescription="@string/content_description"
        android:scaleType="fitCenter" />

</RelativeLayout>